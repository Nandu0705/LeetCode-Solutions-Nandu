class Solution {
public:
    string smallestPalindrome(string s) {
        int n= s.size();
        string s1=s;
        sort(s1.begin(),s1.begin() + n / 2);
        for(int i=0;i<=n/2;i++){
            s1[n-1-i]=s1[i];
        }
        return s1;
    }
};
